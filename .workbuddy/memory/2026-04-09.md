# 2026-04-09 工作日志

## 修改游戏项目支持直接通过 index.html 运行

- 修改 `vite.config.ts`：添加 `base: './'`
- 修改 `src/App.tsx`：BrowserRouter → HashRouter
- 构建成功，dist/index.html 可直接双击打开

## 大批量功能更新（第二~九轮）

详见 MEMORY.md

## 第三轮功能需求（第十轮）

1. **进化按钮（Team.tsx）** - 队伍页详情弹窗新增进化按钮+完整进化动画（5.2s）
2. **仓库按钮悬空（Box.tsx）** - 列表区域 pb-52→pb-56
3. **转盘奖品+红点（Wheel.tsx + Menu.tsx）** - 奖品列表显示概率，CD好了红点提示
4. **个体值道具（Shop + types + store）** - 6种IV道具（3000金/个），useIvItem方法
5. **副本敌人详情（Dungeon.tsx）** - 点击敌方卡片查看详细弹窗
6. **副本行动按钮（Dungeon.tsx）** - pointer-events-auto修复
7. **副本跳过捕捉** - 已确认副本无捕捉环节
8. **副本每5层奖励（Dungeon.tsx + store）** - 可配置奖励系统（第5/10/15...50层）
9. **新手引导（Home.tsx）** - 5步引导系统介绍游戏功能
10. **Boss难度增加（types + Wild.tsx）** - bossLevelBonus: 15/20/25/30/40，Boss IV=25，EV=80/项

## 第四轮功能需求（第十一轮）

1. **转盘奖品显示在转盘上（Wheel.tsx）** - 奖品和概率直接显示在转盘圆盘扇区上，取消下方列表
2. **红点位置修正（Menu.tsx）** - 红点从金币移到"幸运转盘"按钮上，用relative容器包裹
3. **HP复活Bug修复（store/useGameStore.ts）** - 5处hp计算修复：`Math.min(Math.max(pokemon.hp, 0) + ..., newStats.hp)`
4. **新增约25个宝可梦物种（types.ts）** - 派拉斯系、蚊香蝌蚪系、凯西系、腕力系等，扩展技能表
5. **地图扩展到8个（types.ts WILD_MAPS）** - 新增双子岛、华蓝洞窟、冠军殿堂
6. **贴图下载脚本（scripts/download_sprites.mjs）** - 使用ESM+fetch，当前网络不通待后续执行
7. **进化石道具（types.ts + Shop.tsx + Play.tsx）** - 雷/火/水/叶/月之石，价格2100-2500，商店新Tab
8. **培育屋背包（Play.tsx）** - 背包模式显示进化石+个体值道具，支持使用进化石直接进化
9. **进化石SVG图标** - 5个石头道具图标（public/items/）

## 第五轮功能需求

1. **商店界面可滚动（Shop.tsx）** - 商品列表区域改为flex-1+overflow-y-auto，解决分类多时显示不全的问题
2. **设置页面（Settings.tsx）** - 新增设置页面，支持导出存档为.js文件、读取.js存档文件、重置存档
3. **大菜单增加设置入口（Menu.tsx）** - grid中新增设置卡片，灰色调样式
4. **贴图下载脚本更新** - 扩展为全部98个物种，网络不通需用户手动在有网环境运行

## 贴图下载
- 脚本源切换为 jsdelivr CDN（国内可访问）
- 已成功下载 41 个缺失贴图（0 失败），现有 98 物种全部覆盖
- 运行命令: `node scripts/download_sprites.mjs`

## 第六轮功能需求

1. **贴图下载成功** - CDN 切换为 jsdelivr，41个新贴图下载成功
2. **移动端兼容性优化（index.html + index.css）**:
   - viewport 禁止缩放（maximum-scale=1.0, user-scalable=no）
   - overscroll-behavior: none（防 iOS 橡皮筋滚动）
   - touch-action: manipulation（消除 300ms 延迟）
   - -webkit-tap-highlight-color: transparent（去除点击高亮）
   - env(safe-area-inset-*) 适配刘海屏
   - body 禁止文字选择，输入框允许选择

## 第七轮功能需求（副本系统大改）

1. **map6 wildPool修复（types.ts）** - 移除不存在的物种ID（86/87/90/91/124），替换为已有物种
2. **副本一键治愈（Dungeon.tsx）** - 复用 healTeam，费用 = 队伍人数 × 30 金币
3. **副本难度曲线（Dungeon.tsx）** - getDungeonDifficulty 函数，每10层一段（普通/困难/精英/大师/传说），IV/EV/等级倍率递增
4. **副本敌方预览（Dungeon.tsx）** - previewEnemy 函数 + UI 预览卡片（图标+类型+能力值柱状图）
5. **副本敌方信息面板对齐（Dungeon.tsx）** - 与 Wild.tsx 一致：暗色面板(bg-slate-800) + 我方技能属性克制 + 敌方弱点/抗性
6. **构建验证通过** - npm run build 零错误，lint 通过

## 第八轮功能需求

1. **副本每层怪物固定（Dungeon.tsx）** - 使用确定性种子随机数（seededRandom），每层怪物始终相同
2. **商店精灵球精简（Shop.tsx）** - 仅保留精灵球/超级球/高级球/大师球
3. **商店分类滚动修复（Shop.tsx）** - 分类Tab栏添加 touch-action: pan-x 确保移动端可滑动
4. **幸运转盘大改（Wheel.tsx）**:
   - 奖品大幅提升（100~2000金币、稀有糖果×3、伤药×10、糖果×20、500~1000经验）
   - 1%概率获得随机闪光6V个体值宝可梦（1级，加入仓库）
   - 抽奖结果显示优化：弹簧动画+传说大奖特殊样式
5. **满个体值6V标签（全局）** - types.ts 新增 isPerfectIv 函数，Team/Box/Play/Dungeon/Wild 全部同步显示

## 第九轮功能需求

1. **商店分类标签换行（Shop.tsx）** - 从 overflow-x-auto 横向滚动改为 flex-wrap 自动换行
2. **野外/Boss等级固定（Wild.tsx）** - Boss等级改为 mapInfo.levelRange[1] + bossLevelBonus，不再依赖玩家等级
3. **副本敌人等级固定（Dungeon.tsx）** - 等级公式改为 3 + floor*1.5，previewEnemy 移除 playerLevel 参数
4. **野外图鉴预览等级范围修复（Wild.tsx）** - 从 activePokemon.level±2 改为 mapInfo.levelRange

## 第十轮：游戏指南文档

- 编写完整游戏介绍文档 `GAME_GUIDE.md`，涵盖14个章节
- 包含所有核心数值：属性公式、伤害公式、捕捉率公式、道具价格、地图等级、进化链等

## 第十一轮：补全绿毛虫进化链

- **问题**：map1 常磐森林 wildPool 中的 ID 10（绿毛虫）缺少数据和贴图
- **新增 SPECIES_INFO**：10 绿毛虫（虫系）、11 铁甲蛹（虫系）、12 巴大蝶（虫+飞行系）
- **新增技能库**：bug_bite（虫咬）、string_shot（吐丝）、harden（变硬）、psybeam（幻象光线）
- **新增技能学习表**：10→tackle/string_shot/bug_bite，11→tackle/harden/bug_bite，12→tackle/harden/bug_bite/psybeam/poison_sting/confusion/psychic
- **新增 pokemonTypes**：10→虫，11→虫，12→虫+飞行
- **下载贴图**：10/11/12 的 normal + shiny 共6张
- **更新下载脚本**：allSpeciesIds 添加 10, 11, 12





