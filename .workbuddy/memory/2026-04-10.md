# 2026-04-10 工作日志

## Bug修复（本次会话）

### BUG-03 已修复：restoreTeamHunger/Happiness 无论是否需要都收全队费用
- 文件：`src/store/useGameStore.ts`
- 修复：仿照 healTeam 模式，先筛选出实际需要恢复的宝可梦（hunger/happiness < MAX），只对它们收费。若无需恢复直接返回 state。

### BUG-07 已修复：test_addLevel 传入0经验实际不加级
- 文件：`src/store/useGameStore.ts`
- 修复：改为计算当前等级剩余所需经验 `EXP_TO_NEXT_LEVEL(p.level) - p.exp`，传入 applyExp，实现精确升一级。

### BUG-08 已修复：Wild.tsx attack() 中 STAB 恒为1.5、typeMulti 恒为1
- 文件：`src/pages/Wild.tsx`
- 修复：
  - STAB：调用 getPokemonTypes 获取玩家宝可梦属性，判断是否包含技能属性，是则1.5否则1
  - typeMulti：调用 getTypeEffectiveness(skill.type, wildTypes).multiplier 计算实际克制倍率
  - typeMulti 提升为 let 变量到 attack 函数顶部（默认1），避免闭包访问块级 const 的问题
  - 攻击日志增加效果提示（"效果拔群"/"效果不佳"/"没有效果"）

### BUG-09 已修复：捕捉成功后重新生成 nature/ivs 导致与战斗数据不一致
- 文件：`src/pages/Wild.tsx`
- 修复：
  - WildPokemon 类型增加 nature/ivs/evs 字段
  - 生成野生宝可梦时（explore函数）将 nature、ivs、evs 存入 wildPkmn 状态
  - throwPokeball 捕捉时直接使用 wildPkmn.nature / wildPkmn.ivs / wildPkmn.evs，不再重新随机

### BUG-05（UI）已修复：handleRestoreHunger/Happiness 前置金币检查用全队数量
- 文件：`src/pages/Wild.tsx`
- 修复：UI 侧先计算实际需要恢复的宝可梦数量再判断金币是否足够，与 store 逻辑保持一致；全满时给出提示。

### HP复活Bug（丝血复活）：修复了全部8处
- 文件：`src/store/useGameStore.ts`
- 核心逻辑：HP=0（死亡）的宝可梦在任何导致maxHP变化的场景后，必须保持HP=0
- 修复点：
  1. applyExp（第112-114行）：死亡宝可梦升级后保持HP=0；清理冗余Math.min
  2. finishTrain（第409行）：死亡宝可梦特训后maxHP增加不复活
  3. gainRandomEv（第565行）：死亡宝可梦获得EV后不复活
  4. useIvItem（第783行）：死亡宝可梦使用IV道具后不复活（本次会话补修复）
  5. evolvePokemon（第872行）：死亡宝可梦进化后不复活
  6. toggleShiny（第915行）：死亡宝可梦改闪光后不复活
  7. test_toggleShinyFirst（第1021行）：同toggleShiny
  8. changeStats（第530行）：增加Math.floor确保HP为整数，防止浮点精度导致0变0.00xxx

